export default function Settings({flags,setFlags}){const keys=Object.keys(flags);const toggle=k=>setFlags({...flags,[k]:!flags[k]});return (<section className='card'><h3>Feature Switches</h3><p className='sub'>Everything is ON by default. Toggle to simulate kill‑switch behavior. (Stored locally.)</p><div className='list'>{keys.map(k=>(<div className='row' key={k}><div className='title'>{k}</div><button className='btn' onClick={()=>toggle(k)}>{flags[k]?'On':'Off'}</button></div>))}</div></section>)}